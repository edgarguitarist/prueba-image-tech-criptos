/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["randomuser.me", "www.coinlore.com", "c1.coinlore.com"],
  },
}

module.exports = nextConfig
